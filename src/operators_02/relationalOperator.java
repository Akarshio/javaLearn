import static java.lang.IO.*;

void main() {
    // < , > , <= , >= , == , !=

    int userAge = 5;
     if ( userAge != 18){
         println("user not eligible");
     }else {
         println("user eligible");
     }
}